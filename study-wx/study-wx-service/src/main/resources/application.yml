server:
  port: 8088

spring:
  application:
    name: study-wx

study:
  wx:
    app-id: wx-demo-app-id
    app-secret: fake-wx-app-secret
    mock-enabled: true
    code2-session-url: https://api.weixin.qq.com/sns/jscode2session?appid=%s&secret=%s&js_code=%s&grant_type=authorization_code
    user-service-url: http://localhost:9092
    token-ttl-minutes: 120

management:
  endpoints:
    web:
      exposure:
        include: health,info
