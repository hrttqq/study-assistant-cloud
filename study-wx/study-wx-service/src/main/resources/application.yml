server:
  port: 8088

spring:
  application:
    name: study-wx

study:
  wx:
    app-id: wx-demo-app-id
    app-secret: fake-wx-app-secret

management:
  endpoints:
    web:
      exposure:
        include: health,info
